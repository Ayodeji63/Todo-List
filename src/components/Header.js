import React from 'react'

const Header = () => {
  return (
    <div>
        <h1>Todo</h1>
        <div>
            <button>Sign In</button>
            <button>Sign Up</button>
        </div>
    </div>
  )
}

export default Header